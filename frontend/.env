# Feature flag: set to "true" to use localStorage (no backend needed)
# Set to "false" (or remove) to connect to the Laravel API
VITE_USE_LOCAL_STORAGE=false

# Laravel API base URL (only used when VITE_USE_LOCAL_STORAGE=false)
VITE_API_BASE_URL=http://127.0.0.1:8010/api/v1
